apply plugin: 'java-library'
apply plugin: 'maven-publish'

// 配置参数
ext {
    maven_artifactId = 'hualuomoli-codec'
}

apply from: rootProject.file('maven-java.gradle')

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
